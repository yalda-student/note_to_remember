import 'package:flutter/material.dart';

class InvalidState extends StatelessWidget {
  const InvalidState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Invalid State'),
    );
  }
}
